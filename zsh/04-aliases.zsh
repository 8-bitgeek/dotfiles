alias ta='tmux att -t'
alias tn='tmux new -s'
alias tl='tmux ls'
alias pof='unset all_proxy https_proxy http_proxy'
alias vim='nvim'
alias pc='pbcopy'
# alias bt=blueutil --connect "$(blueutil --paired | grep -B1 'not connected' | grep 'name:' | cut -d'"' -f2 | head -1)"
